<UserControl x:Class="O2matic.Views.RegistreEquipmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:O2matic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="cmbEquipmentType" Grid.Row="0" Margin="5,5,0,5" HorizontalAlignment="Left" SelectedIndex="0" Width="770">
            <!--Items will be added dynamically from the database-->
        </ComboBox>

        <TextBox x:Name="txtSerialNumber" Grid.Row="1" Margin="5" HorizontalAlignment="Stretch" />

        <ComboBox x:Name="cmbLocation" Grid.Row="2" Margin="5" HorizontalAlignment="Stretch" SelectedIndex="0">
            <!--Items will be added dynamically from the database-->
        </ComboBox>

        <Button x:Name="btnSubmit" Grid.Row="3" Content="Submit" Margin="5" HorizontalAlignment="Right" Click="btnSubmit_Click"/>
    </Grid>
</UserControl>
